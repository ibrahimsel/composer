[develop]
script_dir=$base/lib/muto_composer
[install]
install_scripts=$base/lib/muto_composer
[tool:pytest]
testpaths = test
# launch_testing tests require `launch_test` runner, not pytest
addopts = --ignore=test/integration
